# 实施顺序脚本清单：SM1

## 顺序
1. 建立文档骨架与任务约束。
2. 扫描 `scout-media-agents` 现有 types / CLI / state 能力。
3. 定义治理对象 schema。
4. 落文件型 registry / run state。
5. 扩 CLI 命令。
6. 跑 `npm run check` 与 CLI smoke。
7. git 提交并推送远程。

## 执行命令建议
```bash
cd /Users/sourcefire/1data/scout-lab/scout-media-agents
npm run check
npm run cli -- topics:list
npm run cli -- review:list
npm run cli -- plan:next --limit=5
npm run cli -- runs:list --limit=5
```

## 回滚点
- 文档层回滚: 删除 `docs/Issue-Checks/2026-05` 新增文件
- 代码层回滚: 回到上一安全提交 `689cc59`
