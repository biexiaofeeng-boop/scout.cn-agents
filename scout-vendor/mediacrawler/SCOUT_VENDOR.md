# Vendor Provenance

This directory is an isolated copy imported into `scout-lab`.

Source copied from:
- `/Users/sourcefire/1data/workspace-ln/BeautyModel-Lab/BeautyQA-vendor/MediaCrawler`

Import intent:
- keep `BeautyModel-Lab` runtime and git history unaffected
- allow `scout-lab` to evolve vendor integration independently
- use this directory as the canonical vendor crawler candidate for future `scout-media-agents` runtime integration

Operational note:
- preserve upstream `LICENSE`
- treat local changes here as `scout-lab` integration changes, not upstream changes
